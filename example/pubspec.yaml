name: do_it_example
description: Example app for the do_it package.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.0.0
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  do_it_kit:
    path: ../

flutter:
  uses-material-design: true
